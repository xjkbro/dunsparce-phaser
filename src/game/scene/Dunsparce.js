import Phaser from "phaser";

export default class Dunsparce extends Phaser.Scene {
    constructor() {
        super("dunsparce");
        this.score = 0;
    }

    preload() {}
    create() {}
    update() {}
}
